<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.biblioapp">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> (opcional) -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Biblioapp">

        <!-- Activity principal -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otras activities -->
        <activity android:name=".ui.SignUpActivity" android:exported="true" />
        <activity android:name=".ui.HomeActivity" android:exported="true" />
        <activity android:name=".ui.ProductDetailActivity" android:exported="true" />
        <activity android:name=".ui.CartActivity" android:exported="true" />

        <!-- Admin activity -->
        <activity android:name=".ui.AdminActivity" android:exported="true" />

    </application>
</manifest>